alias ga="log git add ."
alias gc="log git commit -m"
alias gp="log git push"
alias gr="log git remote -v"

alias bl="log brew list"
alias bu="log brew update"

alias config_apply="log chezmoi_check_apply_config"

alias ssh_show_config="log ssh -G localhost"